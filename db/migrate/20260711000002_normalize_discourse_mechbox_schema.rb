# frozen_string_literal: true

class NormalizeDiscourseMechboxSchema < ActiveRecord::Migration::Current
  def up
    # Retained for existing installs. Schema is created idempotently by earlier migrations.
  end

  def down
  end
end
