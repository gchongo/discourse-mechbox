# Archived MechBox code

Business logic was moved here while the plugin runs in **safe skeleton** mode.

Nothing in this directory is autoloaded by Rails. To restore a feature, move files back under `app/` or `lib/discourse_mechbox/` and wire them in `plugin.rb`.
